# $Id$
request.max=${color}15${bold}REQUEST${bold}${coloroff} -> ${bold}${ircnick}${bold}(${ftpuser}), you have already made ${bold}${reqsmade}${bold} requests in this week.
request.usage=${color}15${bold}REQUEST${bold}${coloroff} -> ${bold}usage${bold}: !request <release>
request.exists=${color}15${bold}REQUEST${bold}${coloroff} -> ${bold}${rdirname}${bold} already been requested
request.error=${color}15${bold}REQUEST${bold}${coloroff} -> Requests folder ${bold}${rdirname}${bold} not found.

reqfilled.usage=${color}15${bold}REQFILLED${bold}${coloroff} -> ${bold}usage${bold}: !reqfilled <release>
reqfilled.error=${color}15${bold}REQFILLED${bold}${coloroff} -> ${bold}${fdirname}${bold} is not requested

requests.header=${color}15${bold}REQUESTS${bold}${coloroff}:
requests.list=${color}15[${coloroff}${num}${color}15]${coloroff} ${reqrequest} by ${bold}${requser}${bold}
requests.footer=${color}15${bold}REQUESTS${bold}${coloroff}: Use !reqfilled <name> after you fill a request.

reqdel.error=${color}15${bold}REQDEL${bold}${coloroff} -> ${bold}${ddirname}${bold} was not found.
reqdel.notowner=${color}15${bold}REQDEL${bold}${coloroff} -> ${bold}${ircnick}${bold}(${ftpuser}) is not owner of ${ddirname}
reqdel.notempty=${color}15${bold}REQDEL${bold}${coloroff} -> ${bold}${ddirname}${bold} is not empty.
reqdel.usage=${color}15${bold}REQDEL${bold}${coloroff} -> ${bold}usage${bold}: !reqdel <request_name>